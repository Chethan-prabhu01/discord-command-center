{
  "name": "discord-command-center",
  "private": true,
  "version": "1.0.0",
  "description": "Root convenience scripts. Backend and frontend each have their own package.json/node_modules.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "register-commands": "npm run register-commands --prefix backend",
    "seed-admin": "npm run seed-admin --prefix backend"
  }
}
